default_stages: [push] # Pushする直前にだけ動かす設定

repos:
  - repo: local
    hooks:
      - id: ai-local-review
        name: AI Local Code Review (Claude)
        entry: ./scripts/local_ai_review.sh # さっき作ったスクリプトを指定
        language: script
        pass_filenames: false
        always_run: true